# Smart Land Copilot — المتطلبات الأساسية
## ── تحليل الكود وتنسيقه ──
pylint>=3.0.0
flake8>=7.0.0
black>=24.0.0
isort>=5.13.0
mypy>=1.8.0

# ── فحص الأمان ──
bandit>=1.7.5
safety>=3.0.0

# ── اختبارات الوحدة والتكامل ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
hypothesis>=6.100.0

# ── اختبار التحميل والأداء ──
locust>=2.20.0

# ── قاعدة البيانات وORM ──
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.19.0

# ── الخادم وAPI ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── واجهة المستخدم والخرائط ──
streamlit>=1.32.0
folium>=0.15.0
streamlit-folium>=0.15.0
geopy>=2.4.0

# ── الأمان والمصادقة ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── أدوات مساعدة وتسجيل ──
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.5.0
structlog>=24.1.0

# ── التوثيق (اختياري حسب الحاجة) ──
mkdocs>=1.5.0

# ── أدوات ما قبل الالتزام (Git hooks) ──
pre-commit>=3.6.0