# أدوات التدقيق والأمان المطلوبة
# تثبيت: pip install -r requirements_audit.txt

# ── تحليل الكود ──
pylint>=3.0.0
flake8>=7.0.0

# ── فحص الأمن ──
bandit>=1.7.5
safety>=3.0.0

# ── اختبارات ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# ── اختبار التحميل ──
locust>=2.20.0

# ── قاعدة البيانات ──
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
aiosqlite>=0.19.0

# ── API ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── واجهة المستخدم ──
streamlit>=1.32.0
folium>=0.15.0
streamlit-folium>=0.15.0

# ── الخرائط والجغرافيا ──
geopy>=2.4.0

# ── الأمان ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# ── مساعدات ──
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.5.0